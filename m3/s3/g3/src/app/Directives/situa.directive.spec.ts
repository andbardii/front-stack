import { SituaDirective } from './situa.directive';

describe('SituaDirective', () => {
  it('should create an instance', () => {
    const directive = new SituaDirective();
    expect(directive).toBeTruthy();
  });
});
